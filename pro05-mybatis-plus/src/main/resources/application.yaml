spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_ssm?serverTimezone=UTC
    username: root
    password: jing
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

  global-config:
    db-config:
      # 设置统一的主键生成策略
      id-type: auto
  type-aliases-package: com.lee.mybatisplus.bean
#      # 设置实体类所对应的表的统一前缀
#      table-prefix: t_
